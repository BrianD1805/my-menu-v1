# Orduva Ver-0.046 handover

This patch fixes the Netlify type error in `components/menu/ProductCard.tsx` by changing the remaining cart lookup from `item.id` to `item.productId`.

Also bumps the visible in-app version to `Ver: 0.046`.
