# Orduva Ver-0.141B — True viewport cart push reminder modal

- Renders the cart-entry push reminder through a React portal into document.body.
- Prevents the modal from being constrained by storefront/header/card containers.
- Uses a high z-index full-viewport overlay and centred card.
- Keeps push reminder behaviour unchanged.
