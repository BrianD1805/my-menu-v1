# Orduva Ver-0.204B — Tenant settings payment type fix

Fixes local build failure where storefront payment option code referenced newer payment settings fields that were missing from the TenantSettings TypeScript type.

No Supabase SQL required.
