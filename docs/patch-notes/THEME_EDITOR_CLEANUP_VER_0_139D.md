# Orduva Ver-0.139D — Theme editor duplicate preview/suggested cleanup

- Desktop: removes per-section Preview/Suggested quick buttons because the sticky right-hand preview and suggested colours panel are already visible.
- Mobile: keeps the top per-section Preview/Suggested popup buttons and centres them.
- Mobile/tablet: hides the duplicate bottom/right preview and suggested panels so users use the popups instead.
- Keeps palette icon colour picker, one-open accordion behaviour, and section save buttons.
