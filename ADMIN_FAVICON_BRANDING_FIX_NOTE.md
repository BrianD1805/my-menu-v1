# Orduva Ver-0.130B Admin favicon branding fix

This patch makes the tenant admin use the Orduva root/platform favicon and icon assets consistently across:

- app metadata
- admin route metadata
- admin head links
- admin PWA manifest
- dynamic root manifest admin branch
- legacy admin icon filenames, overwritten with Orduva platform icons for cached references

No storefront, order, customer, push, onboarding, wildcard, or admin functional logic was changed.
